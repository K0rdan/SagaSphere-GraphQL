apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "SagaSphere_GraphQL.fullname" . }}-test-connection"
  labels:
{{ include "SagaSphere_GraphQL.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "SagaSphere_GraphQL.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

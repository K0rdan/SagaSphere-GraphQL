apiVersion: v1
kind: Service
metadata:
  name: {{ include "SagaSphere_GraphQL.fullname" . }}
  labels:
{{ include "SagaSphere_GraphQL.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "SagaSphere_GraphQL.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
